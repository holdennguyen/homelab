apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: hello-demo
  name: hello-demo
spec:
  ports:
  - name: http
    port: 80
    targetPort: 80
  selector:
    app.kubernetes.io/name: hello-demo
  type: NodePort